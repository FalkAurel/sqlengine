# Dataframe

The Dataframe is a logical structure for allowing easy access to the underlying memory in terms of Columns within a Table.


## Memory Layout

The primary job of the `Dataframe` is to unify and manage `Block`s.
